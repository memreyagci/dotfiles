#!/bin/sh

volume="$(pactl list sinks | grep '^[[:space:]]Volume:' | \
    head -n $(( $SINK + 1 )) | tail -n 1 | sed -e 's,.* \([0-9][0-9]*\)%.*,\1,')%%"

if [ "$(pactl list sinks | grep 'Mute: yes')" ];
then
    printf "Vol: muted"
else
    printf "Vol: $volume"
fi
