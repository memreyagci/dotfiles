#!/bin/sh

alias \
    cp="cp -iv" \
    crepos="cd $HOME/documents/repos" \
    dotfiles="/usr/bin/git --git-dir=$HOME/documents/repos/dotfiles --work-tree=$HOME" \
    irssi="irssi --config=$HOME/.config/irssi/config" \
    ls="exa -l --group-directories-first --color=auto --group" \
    mkdir="mkdir -pv" \
    mv="mv -iv" \
    newsboat="newsboat; pkill -RTMIN+11 dwmblocks" \
    ovim="nvim \$(fzf -e)" \
    rm="rm -vI" \
    um="udiskie-mount" \
    uu="udiskie-umount" \
    v="nvim" \
    vea=". venv/bin/activate" \
    ved="deactivate" \
    wget="wget --hsts-file=$XDG_CACHE_HOME/wget-hsts" 
